{{ attach_library('droopler_theme/footer-primary') }}

{% set base_class = 'footer-primary' %}

<div {{ bem(base_class) }} data-theme="theme-dark">
  <div {{ bem('content', [], base_class, ['container']) }}>
    <div {{ bem('content-wrapper', [], base_class, ['row']) }}>
      {% block columns %}
      {% endblock %}
    </div>
  </div>
</div>
