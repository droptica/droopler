{#
/**
 * @file
 * Template for a pager.
 */
#}
{{ attach_library('droopler_theme/pagination') }}

{% include '@radix/pagination/pagination.twig' with {
  items: items,
  alignment: 'center',
  utility_classes: 'mt-5'
} %}
