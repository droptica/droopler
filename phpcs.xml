<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="droopler">
  <description>PHP CodeSniffer configuration for Droopler profile.</description>

  <arg name="extensions" value="php,inc,install,module,yaml,yml,twig,theme,test,profile,info,scss"/>
  <arg name="report" value="full,gitblame"/>
  <arg name="colors"/>
  <arg name="parallel" value="8"/>

  <config name="installed_paths" value="../../drupal/coder/coder_sniffer/Drupal,../../drupal/coder/coder_sniffer/DrupalPractice,../../slevomat/coding-standard" />

  <file>droopler.info.yml</file>
  <file>droopler.install</file>
  <file>droopler.profile</file>
  <file>src</file>
  <file>modules/custom</file>
  <file>themes/custom</file>

  <exclude-pattern>*/vendor/*</exclude-pattern>
  <exclude-pattern>*/node_modules/*</exclude-pattern>
  <exclude-pattern>*/modules/contrib/*</exclude-pattern>
  <exclude-pattern>*/themes/contrib/*</exclude-pattern>
  <exclude-pattern>*/themes/custom/*/build/*</exclude-pattern>
  <exclude-pattern>*/themes/custom/*/dist/*</exclude-pattern>
  <exclude-pattern>*/themes/custom/*/STARTERKIT_*</exclude-pattern>
  <exclude-pattern>*/src/Generators/*</exclude-pattern>
  <exclude-pattern>*/themes/custom/*/css/*</exclude-pattern>
  <exclude-pattern>*/themes/custom/*/assets/*</exclude-pattern>
  <exclude-pattern>*pattern-lab/*</exclude-pattern>
  <exclude-pattern>*_twig-components/*</exclude-pattern>
  <exclude-pattern>*pl-sk/*</exclude-pattern>
  <exclude-pattern>*interactive_ui/react_slider/js*</exclude-pattern>

  <!-- Exclude unsupported file types. -->
  <exclude-pattern>*.gif</exclude-pattern>
  <exclude-pattern>*.png</exclude-pattern>
  <exclude-pattern>*.svg</exclude-pattern>
  <exclude-pattern>*.md</exclude-pattern>
  <exclude-pattern>*.txt</exclude-pattern>

  <!-- Minified files don't have to comply with coding standards. -->
  <exclude-pattern>*.min.css</exclude-pattern>
  <exclude-pattern>*.min.js</exclude-pattern>

  <rule ref="Drupal">
    <!-- Profile ships explicit version: keys; drupal.org packaging auto-key sniff is noise. -->
    <exclude name="Drupal.InfoFiles.AutoAddedKeys"/>
  </rule>
  <rule ref="DrupalPractice" />

  <!-- Views OPML/RSS templates legitimately emit `<?xml ?>` (XML, not PHP). -->
  <rule ref="Generic.PHP.DisallowShortOpenTag">
    <exclude-pattern>*/views-view-opml.html.twig</exclude-pattern>
    <exclude-pattern>*/views-view-rss.html.twig</exclude-pattern>
  </rule>

  <!-- Require the strict types declaration in every PHP file. -->
  <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes"/>

  <!-- Require nullable types to be declared as such. -->
  <rule ref="SlevomatCodingStandard.TypeHints.NullableTypeForNullDefaultValue"/>

  <!-- Forbid annotations which are not included in the Drupal coding standard but are often added by IDEs. -->
  <rule ref="SlevomatCodingStandard.Commenting.ForbiddenAnnotations">
    <properties>
      <property name="forbiddenAnnotations" type="array">
        <element value="@author"/>
        <element value="@created"/>
        <element value="@copyright"/>
        <element value="@license"/>
        <element value="@package"/>
        <element value="@version"/>
      </property>
    </properties>
  </rule>

  <!-- Forbid documentation auto-generated by IDEs which does not align with the Drupal documentation standards. -->
  <rule ref="SlevomatCodingStandard.Commenting.ForbiddenComments">
    <properties>
      <property name="forbiddenCommentPatterns" type="array">
        <element value="/@inheritDoc/"/>
        <element value="/^Class [a-zA-z]*\.$/"/>
        <element value="/^Interface [a-zA-z]*\.$/"/>
        <element value="/^Trait [a-zA-z]*\.$/"/>
        <element value="/^Enum [a-zA-z]*\.$/"/>
        <element value="/^[a-zA-z]* constructor\.$/"/>
      </property>
    </properties>
  </rule>

  <!-- Enforce correct formatting of return types hints. -->
  <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHintSpacing"/>

  <!-- Use statements should be ordered alphabetically. -->
  <rule ref="SlevomatCodingStandard.Namespaces.AlphabeticallySortedUses">
    <properties>
      <property name="caseSensitive" value="true"/>
    </properties>
  </rule>
</ruleset>
