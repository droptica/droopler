info:
  # For images this must be set to media_image, it is not equal to media's bundle.
  type: media_image

fields:
  # RAW field data to be sent to Entity create. Specify "target_id" and "alt" here.
  field_media_image:
    data:
      - target_id: 9
        alt: Centurion
