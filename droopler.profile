<?php

/**
 * @file
 * Procedural shim for the Droopler profile.
 *
 * All hook implementations now live in \Drupal\droopler\Hook\Hooks. This
 * file is intentionally minimal — Drupal still requires a `.profile` file
 * to exist for the profile to be recognised by the extension list, but no
 * procedural code is needed here.
 */

declare(strict_types=1);
